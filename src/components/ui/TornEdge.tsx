/** The receipt-tape signature element: a torn-paper strip. Place directly
 * beneath a card's body (same background color) to close it off. Reserved
 * for the item-review and summary cards only — see DESIGN direction. */
export function TornEdge() {
  return <div className="torn-edge" aria-hidden="true" />
}
